{
  "name": "luxe-ma-shop",
  "version": "1.0.0",
  "description": "Boutique accessoires de mode - LUXE.MA",
  "scripts": {
    "start": "pm2-runtime start backend/server.js -i max",
    "install:backend": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
