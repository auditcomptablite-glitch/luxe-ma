
{
  "name": "luxe-ma-shop",
  "version": "1.0.0",
  "description": "Boutique accessoires de mode - LUXE.MA",
  "scripts": {
    "start": "node backend/server.js",
    "install:backend": "cd backend && npm install",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
